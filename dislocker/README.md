trying to mount a bitlocker drive, I was getting an error

I tried to use this docker to get the latest verion, built by git, then extract the binaries out to my host system

this still didn't work.

turns out ubuntu was able to automount the image itself

